//no fuckin wifi so i can't text anyone so i code

var i = 0;
var text = "Tu es fini?";
var output = "" // var output; would give the output but with undefined.

while (i < text.length) {

    output += (text[i]);  //  same as: output = output + text[i]
    console.log(text[i]);
    i++;

}

console.log(output);

//Voila!
